<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gigass.trading.repository.HotSearchRecordRepository">

    <insert id="insertBatch" parameterType="java.util.List">
        INSERT INTO hot_search_records (source, title, content, heat_score, sentiment_score, propagation_speed, verification_score, total_score, created_time, updated_time, status)
        VALUES
        <foreach collection="records" item="record" separator=",">
            (#{record.source}, #{record.title}, #{record.content}, #{record.heatScore}, #{record.sentimentScore}, #{record.propagationSpeed}, #{record.verificationScore}, #{record.totalScore}, #{record.createdTime}, #{record.updatedTime}, #{record.status})
        </foreach>
    </insert>

</mapper> 